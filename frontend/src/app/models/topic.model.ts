export class Topic {
       closed: boolean;
       content: string;
       creation_date: string;
       last_update_date: string;
       title: string;
       views: number;

       constructor() {}
}
